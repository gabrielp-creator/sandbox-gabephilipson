import { Audiowide, Abel } from 'next/font/google';

export const audiowide = Audiowide({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-audiowide',
});

export const abel = Abel({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-abel',
});
